"""r2d3_eval — deterministic scoring CLI for the R2D3 V1 evaluation suite.

CLI entry will be exposed as `r2d3-eval` once packaged. See `cli.py` for the
typer/argparse front-end (to be added).
"""
__version__ = "0.0.0"
