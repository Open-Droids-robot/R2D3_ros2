"""Ready-made platform interfaces built on the R2D3 facade.

    rl_env    R2D3Env — a Gymnasium environment (RL pipelines)
    vlm_loop  PerceptionLoop — grab camera -> model -> high-level action (VLM)
    teleop    TeleopServer — stream target joint/EE poses (teleoperation)
"""
